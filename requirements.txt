# Requires Python 3.14 or newer (enforced by pyver.py).
#
# Runtime dependency for the local dashboard host (app.py).
# The simulator, codec, engine, consumer, timestamping, and tests use only the
# Python standard library — no third-party packages required for those.
flask>=3.0
