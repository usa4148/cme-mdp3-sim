# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environment
.venv/
venv/

# Generated artifacts (regenerate with build_dashboard.py)
session.json
dashboard.html

# Cached prior-session closes (re-fetched by settlement.py)
.prior_close_cache.json

# CME's full config.xml — download it yourself (CME login required); the
# committed schema/config.sample.xml is the fallback.
schema/config.xml

# OS
.DS_Store
